# 생성된 문제
> 2026-04-04T12-48-47

---

## 1. 아이디어 셀렉터 (Idea Maker)

### 수학적 상황
- **수학적 객체**: 최고차항의 계수가 1인 사차함수 f(x)

- **제약 A가 부과하는 것**: f(−1) = 0이고 f(3) = 0이다. 이는 f(x) = (x+1)(x−3)Q(x) (Q는 이차식)로의 인수분해를 허용하지만, 이차인수 Q(x) = x² + ex + g의 두 계수 e, g가 미결이다. **자유도 2.**

- **제약 B가 부과하는 것**: f′(1) = 0이고 f(1) = −16이다. 이는 x = 1에서 수평접선을 가지며 그 접선의 y좌표가 −16임을 뜻한다. 사차함수의 4개 계수 중 c, d를 a, b로 표현하지만, a와 b는 미결이다. **자유도 2.**

- **공유 파라미터**: a와 b (또는 동치적으로, 이차인수 Q의 계수 e와 g). 제약 A는 (c, d)를 (a, b)의 일차식으로 결정하고, 제약 B도 (c, d)를 (a, b)의 **다른** 일차식으로 결정한다. 두 결정이 양립하려면 a와 b에 대한 연립일차방정식이 성립해야 한다.

- **강제 결과**: A+B를 동시에 적용하면 a = −4, b = 6, c = −4, d = −15가 유일하게 결정되어 **f(x) = (x−1)⁴ − 16**이 된다. 두 근 −1과 3은 x = 1에 대해 대칭이고, 나머지 이차인수 x² − 2x + 5 = (x−1)² + 4도 x = 1에 대해 대칭이므로, f(x) 전체가 x = 1을 축으로 하는 완전한 짝함수 구조(평행이동된 네제곱)를 가진다. 이는 어느 제약 단독으로도 예측할 수 없는 결과이다.

- **영감 source_pid**: **P004** (등도함수값 이근의 대칭 결정 — 두 근의 중점에서의 도함수 조건이 나머지 인수의 대칭축을 정렬시키는 메커니즘의 사차 확장), **P013** (삼차함수 임계값의 이중근 구조 — 임계값에서의 수준집합 f(x) = f(1)이 사중근을 가지는 극단적 중복도 구조)

---

### 수험생의 발견 순간
1. **첫 시도**: 제약 A를 전개한다. f(−1) = 0, f(3) = 0으로부터 c = −20 − 7a − 2b, d = −21 − 6a − 3b를 얻는다. 또는 f(x) = (x+1)(x−3)(x² + ex + g)로 놓고 전개하여 a = e−3, b = g−3e, c = −3g, d = 0… 이 아니라 상수항 확인이 필요하다. 어느 쪽이든 자유 파라미터 2개가 남는다.

2. **막히는 순간**: "a, b가 자유라서 f가 하나로 결정되지 않는다. 극점 조건을 써야 하는데, f′(1) = 0이 a와 b를 어떻게 동시에 잡아줄 수 있지?"

3. **제약 B 적용**: f′(1) = 4 + 3a + 2b + c에 제약 A의 결과 c = −20 − 7a − 2b를 대입한다.
   f′(1) = 4 + 3a + 2b + (−20 − 7a − 2b) = **−16 − 4a**.
   여기서 결정적 발견: **b가 소거된다!** f′(중점)을 계산할 때 b의 기여가 정확히 상쇄되어, f′(1) = 0은 오직 **a = −4**만을 강제한다.

4. **발견**: "b가 사라진 이유는 x = 1이 두 근 −1, 3의 정확한 중점이기 때문이다. 중점에서 (x+1)(x−3)의 기울기 기여가 대칭적으로 상쇄되어, 도함수가 이차인수 Q의 정보만 남긴다. a = −4를 얻은 뒤 f(1) = −16에 대입하면 b = 6이 즉시 결정되고, **f(x) = (x−1)⁴ − 16**이라는 완전네제곱 구조가 드러난다. 네 조건이 모두 이 하나의 대칭 형태를 가리키고 있었다!"

---

## 2. 프라블럼 메이커 (Problem Maker)

### 문제 초안
> 최고차항의 계수가 $1$인 사차함수 $f(x)$가 다음 조건을 만족시킨다.
>
> **(가)** $f(x)$는 $x^2 - 2x - 3$으로 나누어떨어진다.
>
> **(나)** 곡선 $y = f(x)$ 위의 점 $\bigl(1,\, f(1)\bigr)$에서의 접선의 방정식이
> $\quad\;$ $y = -16$이다.
>
> $f(5)$의 값을 구하시오. $\quad$ [4점]

---

##

### 조건-상황 대응표
- **(가)** → 제약 A: $f(-1)=0$, $f(3)=0$을 나눗셈 조건으로 위장. 근의 중점이 $1$이라는 사실은 이차식 계수 속에 매몰.
- **(나)** → 제약 B: $f'(1)=0$과 $f(1)=-16$을 접선 방정식 하나로 통합. $x=1$이 극점이라는 기하적 정보만 표면에 노출.
- **발문 $f(5)$** → 강제 결과 $f(x)=(x-1)^4-16$ 확인 후 대입을 유도.

---

## 3. 디테일 빌더 (Detail Builder)

### 완성된 문제
> 최고차항의 계수가 $1$인 사차함수 $f(x)$가 다음 조건을 만족시킨다.
>
> **(가)** $f(x)$는 $x^{2}-2x-3$으로 나누어떨어진다.
>
> **(나)** 곡선 $y=f(x)$ 위의 점 $\bigl(1,\;f(1)\bigr)$에서의 접선의 방정식이
> $\quad\;y=-16$이다.
>
> $f(5)$의 값을 구하시오. $\quad$ [4점]

$$\boxed{240}$$

---

## 4. 이벨류에이터 (Evaluator)

### 원본 문제 A
## Problem

**4.**

세 상수 $a(a \neq 0)$, $b$, $c$에 대하여 함수

$$f(x) = ax^3 + bx^2 - cx + c$$

가 다음 조건을 만족시킬 때, $f(3)$의 값을 구하시오.

> (가) $f(1) = 0$, $f'(1) = 1$
> (나) $f(k) = 0$, $f'(k) = -\dfrac{f(-1)}{2}$인 실수 $k(k \neq 1)$이
> 존재한다.

### 원본 문제 B
## Problem

**13.**

$0 < a < 6 < b$인 두 상수 $a$, $b$와 최고차항의 계수가 $-1$인 삼차함수 $f(x)$에 대하여 $f(0) = 20$이고 부등식

$$f(a) \leq f(x) \leq f(6)$$

의 해가 $0 \leq x \leq b$일 때, $f\left(\frac{a+b}{2}\right)$의 값을 구하시오.

### 채점
| 항목 | 점수 | 근거 |
|------|:----:|------|
| **융합성** | **5**/5 | 1-2 얽힘 등급: 강한 얽힘 (4개 O). $c$ 등치에서 $b$ 소거라는 단독 전개에 없던 등식이 출현하고, 이것이 풀이의 핵심 전환 지점. |
| **참신함** | **5**/5 | 원본 A는 삼차함수·명시적 조건($f(1)=0, f'(1)=1$)·다른 근 $k$ 탐색 구조. 원본 B는 삼차함수·부등식 해집합 구조. 완성 문제는 사차함수·나눗셈 조건+접선 조건·중점 대칭 메커니즘으로 객체·조건 구조·풀이 전략이 모두 상이. 원본 어느 쪽의 전략으로도 직접 풀 수 없음. |
| **정합성** | **5**/5 | 다항식 나눗셈, 접선 방정식, 미분 — 모두 고교 범위 내. "나누어떨어진다"는 수능 기출 표현. 서술이 간결하고 자연스러움. |
| **논리성** | **5**/5 | 유일해 확인(1-4: O). 모든 조건 필수(1-5: 양쪽 제거 시 답 변경). 모순·과잉·부족 없음. |
| **풀이 깊이** | **5**/5 | 전환 지점 2회: ①$b$ 소거(중점 대칭 발견), ②$(x-1)^4-16$ 구조 인식. 설계 의도의 핵심 발견(중점 대칭에 의한 파라미터 상쇄)이 풀이에 실제로 나타남. |
| **총점** | **25/25** | |

---

###