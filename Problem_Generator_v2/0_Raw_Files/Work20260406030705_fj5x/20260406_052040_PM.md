[THINKING]
Let me analyze the mathematical situation and previous attempts to craft a new problem.

The mathematical situation involves:
- A cubic function $f(x) = x^3 + ax^2 + bx$ with $f(0) = 0$
- $H(x) = \int_0^x f(t)\,dt$
- Constraint A: $H(x) \ge 0$ for all $x > 0$
- Constraint B: $H(\alpha) = 0$ for some positive $\alpha$
- The hidden fact: These two constraints together force a unique solution via a perfect square inequality

Previous attempts all used cubic functions with conditions like:


1. Tangent lines and extreme values
2. Composition $f(f(x)) = 0$
3. Integration and derivatives at roots
4. Tangent line meeting $y=x$

Now I need to find a natural way to present the integral condition. The core setup is the cubic $f(x) = x^3 + ax^2 + bx$ with the integral $H(x) = \int_0^x f(t)\,dt$ staying non-negative for all positive $x$, while also hitting zero at some specific positive value $\alpha$. The elegant part is that these two conditions together uniquely determine the coefficients through a perfect square constraint. I'm settling on a design where the inequality and equality conditions work together to force a unique cubic through a perfect square mechanism—this is fundamentally different from my previous approaches that relied on algebraic roots or geometric intersections. The setup uses a cubic with leading coefficient 1 and $f(0) = 0$, with the integral constraint $\int_0^x f(t)\,dt \ge 0$ for $x > 0$ paired with $\int_0^{3} f(t)\,dt = 0$ to create the uniqueness, though I'm noticing this presentation might make the structure too transparent to students.

I'm reconsidering how to phrase condition (나) less directly—maybe framing it as a property of the antiderivative function rather than stating the integral value outright—but I realize that if the antiderivative touches zero at $x = 3$ while staying non-negative, that point must be a minimum, which would force the derivative to vanish there too. I need to be careful about what I'm implicitly revealing through the conditions.

Now I'm working out the specific form of $f(x)$. Factoring gives me $x$ times a quadratic, and I'm calculating the discriminant to find the other roots—it works out to $\frac{9}{4}$, which gives clean rational roots.

So the factorization is $f(x) = x(x - \frac{3}{2})(x - 3)$. Now I'm integrating to find $H(x)$ and simplifying it into the form $\frac{x^2(x-3)^2}{4}$.

For positive $x$, this integral is always non-negative and equals zero at $x = 3$. Now I'm thinking about how to structure the problem itself—whether to present it directly with the conditions that the integral stays non-negative for all positive $x$ and equals zero at $x = 3$, then ask for $f(5)$.

The direct approach feels clean, though I could also frame it using an auxiliary function $g(x)$ for the integral and state conditions about where it's non-negative and where it vanishes. But that might obscure the key insight: recognizing that when an integral is always non-negative yet touches zero at a specific point, the integrand must have a zero there. The algebra that follows from this observation is where the real work lies. If $H(\alpha) = 0$ and $H(x) \ge 0$ for all positive $x$, then $\alpha$ must be a minimum point where $H$ touches zero, which means $H'(\alpha) = f(\alpha) = 0$. So $f$ has roots at both $0$ and $\alpha$, giving me $f(x) = x(x^2 + ax + b)$. From the condition that $\alpha$ is a root of the quadratic factor, I can express $b$ in terms of $\alpha$ and $a$, then substitute into the integral condition $H(\alpha) = 0$ to get another constraint.

Working through the algebra, I divide the integral equation by $\alpha^2$ and substitute the expression for $b$, which simplifies to a linear equation in $a$. This gives me $a = -\frac{3\alpha}{2}$ and consequently $b = \frac{\alpha^2}{2}$, so the cubic factors as $f(x) = x(x^2 - \frac{3\alpha}{2}x + \frac{\alpha^2}{2})$.

The quadratic factor has a positive discriminant, yielding roots at $\frac{\alpha}{2}$ and $\alpha$, so $f(x) = x(x - \frac{\alpha}{2})(x - \alpha)$ and $H(x) = \frac{x^2(x - \alpha)^2}{4}$. The entire solution structure is determined by the single parameter $\alpha$—without specifying its value or providing an additional constraint, I have a family of solutions rather than a unique answer.

I could pin down $\alpha$ either by choosing a specific value directly or by imposing another condition, like setting $f'(0)$ to some value. Since the original problem context has $\alpha = 3$, I'll use that and present the solution cleanly.

Now I'm setting up the cubic function with leading coefficient 1 and $f(0) = 0$, then defining $g$ as its integral. The conditions state that $g(x) \geq 0$ for all positive $x$ and $g(3) = 0$. To solve this, I'll express $f$ in terms of its unknown coefficients, compute $g$ explicitly, and use the constraint $g(3) = 0$ to establish relationships between those coefficients. Since $\frac{x^2}{12} > 0$ for positive $x$, I need $h(x) = 3x^2 + 4ax + 6b$ to be non-negative for all $x > 0$. Using the condition $g(3) = 0$, I can derive that $b = -\frac{9+4a}{2}$, and substituting this back into $h(x)$ gives me a quadratic in terms of $a$ that I need to analyze.

For a upward-opening quadratic to stay non-negative on the positive reals, either the discriminant must be non-positive, or both roots must be non-positive. Let me compute the discriminant in terms of $a$.

The discriminant factors as $4(2a+9)^2$, which is always non-negative. Now I need to find the roots: when $a \ge -9/2$, they're $x = 3$ and $x = \frac{-4a-9}{3}$. Since one root is always 3 (which is positive), I need the other root to also be non-positive for $h(x) \ge 0$ on $(0, \infty)$. I'm realizing there's an issue with uniqueness here—multiple values of $a$ could satisfy the constraint $a \ge -9/4$. Let me reconsider the boundary case: when $a = -9/4$, the second root lands exactly at 0, which is the boundary of our domain. For $h(x) \geq 0$ across all positive $x$, since the parabola opens upward, I need the other root to be non-positive, which means $a \geq -9/4$. When $a$ is strictly greater than $-9/4$, the second root sits below zero and $h(x)$ stays non-negative for $x \geq 0$ with $h(3) = 0$. This suggests there's actually a valid range of $a$ values, so either I'm misinterpreting the original problem or there's something off about how it was stated.

Let me reconsider the actual form of $H(x)$ — it factors as $\frac{x^2}{4}$ times a quadratic $h(x) = x^2 + \frac{4a}{3}x + 2b$, and the constraint is that this quadratic stays non-negative for all positive $x$. Using $H(3) = 0$, I get $9 + 4a + 2b = 0$.

Substituting this back, $h(x) = x^2 + \frac{4a}{3}x - (9+4a)$, which indeed gives $h(3) = 0$. Now I'm checking the discriminant to understand when $h(x) \geq 0$ for all $x > 0$ — it simplifies to $\frac{4(2a+9)^2}{9}$, which is always non-negative.

Finding the roots of $h(x)$, I get $x = 3$ and $x = -\frac{4a+9}{3}$ when $a \geq -\frac{9}{2}$. I need to determine when both roots are non-positive so that $h(x) > 0$ for all positive $x$.

The second root must satisfy $-\frac{4a+9}{3} \leq 0$, which means $a \geq -\frac{9}{4}$. But when $a = -\frac{9}{4}$, the second root equals 0, making $h(x) = x(x-3)$ negative on the interval $(0, 3)$, which violates the requirement.

Since $h$ is an upward-opening parabola with roots at $r$ and 3, it's negative between them. For $h(x) \geq 0$ on all positive $x$, I need the second root to be non-positive, and specifically $r < 0$ to avoid negativity in $(0, 3)$.

Wait, that's backwards—if $r < 0$, then $(r, 3)$ includes the entire interval $(0, 3)$, making $h$ negative there. So the only way to satisfy the constraint is if 3 is a double root.

Setting the discriminant to zero gives $a = -9/2$ and $b = 9/2$, so $h(x) = (x-3)^2$ and $H(x) = \frac{x^2(x-3)^2}{4}$. The solution is unique because the upward-opening parabola with a root at 3 must have that root be a double root to stay non-negative on the positive reals.

I'm realizing my earlier mistake—even if the other root were negative, $h$ would still dip below zero between the roots, which includes positive values. So the problem structure forces this elegant constraint. The two conditions together are simple but the deduction is non-trivial. This approach differs from previous attempts by using an integral condition on $g(x) = \int_0^x f(t)\,dt$ rather than explicit conditions on $f$ itself. Now I'm computing $f(5) = 125 - 90 = 35$.

I'm weighing whether to ask for $f(5)$ directly, or explore other values like $f(2) = -1$ or $f(-1) = -10$, or even something involving the integral itself. For a clean 수능-style question, $f(5) = 35$ feels right. But I'm also reconsidering whether to fix the value 3 in condition (나) or leave it as a variable—if I just say a positive $\alpha$ exists satisfying $g(\alpha) = 0$, then the answer depends on what $\alpha$ actually is.

The issue is that if $\alpha$ is left free, then $f(x) = x(x - \alpha/2)(x - \alpha)$ works for any positive $\alpha$, which means there's no unique answer. I need to either specify $\alpha$ directly, or add another constraint that pins it down. Adding a second condition—like specifying an extreme value or the derivative at a particular point—would be more elegant than just stating $\alpha = 3$ outright.

Computing the derivative, I get $f'(x) = 3x^2 - 3\alpha x + \frac{\alpha^2}{2}$, so $f'(0) = \frac{\alpha^2}{2}$. If I require $f'(0) = \frac{9}{2}$, that gives $\alpha = 3$, but it feels arbitrary. Alternatively, I could try making $x = \alpha$ a local minimum, but checking the derivative there shows $f'(\alpha) = \frac{\alpha^2}{2} > 0$, so $\alpha$ isn't even a critical point—that won't work. I think the cleanest approach is just to set $\alpha = 3$ directly and let the inequality plus equality conditions provide the uniqueness.

Now I'm finalizing the problem statement with $\alpha = 3$. The setup is a cubic with leading coefficient 1 and $f(0) = 0$, where the integral $g(x)$ satisfies $g(x) \ge 0$ for all positive $x$ and $g(3) = 0$. This should be sufficient for a well-posed problem, though I'm wondering if it needs more complexity to feel like a full exam question.

Let me verify the calculation for $f(5)$ works out to a clean answer—computing it gives 35, which seems reasonable for this type of problem.

Actually, I'm noticing the requirements say I shouldn't finalize numerical details at this stage, so let me focus on drafting the problem structure instead. Looking back at what I've tried before, I've used various combinations like cubics with tangent lines, extreme values, compositions, and roots with integrals. This approach is different because it introduces an integral function as its own object and uses an inequality condition as the forcing mechanism—the key insight being that an inequality combined with an equality constraint produces a perfect square, which is genuinely distinct from my previous attempts.

Now I'm laying out the differentiation clearly: the conditions shift from pure equations and geometric properties to inequality plus equality, the hidden structure moves from direct observations to something concealed within the inequality, and instead of working with just a cubic function, I'm establishing a relationship between a cubic and an integral function. Let me structure the problem setup starting with the leading coefficient...

I'm defining a cubic function with leading coefficient 1 and f(0)=0, then introducing its integral function. The strategy is to place one constraint as an inequality for positive x and another as an equality at a specific point, which forces a quadratic expression to become a perfect square, uniquely determining the coefficients.

Now I'm considering whether specifying the integral equals zero at a particular point is too direct—it does immediately guide students toward computing that integral to find a relationship between the coefficients, though combining it with the inequality constraint is where the real difficulty lies. the cleanest approach. It's a concrete condition that naturally leads to the computation. I could frame it differently — like describing the area enclosed by the curve and the x-axis for $x > 0$ — but that gets too convoluted and changes the underlying mathematics. So I'll stick with $g(3) = 0$ as my second condition. It's direct and feels like a 수능 problem. I could also just state $\int_0^3 f(x)\,dx = 0$ without introducing $g$, but defining $g$ makes condition (가) feel more natural and cohesive. Two conditions total should be enough — elegant and sufficient, with the real depth coming from how they interact.

---

[OUTPUT]
## [0] 이번 시도의 차별성 선언

- **조건 형태:** 등식·극값·접선 조건 → **부등식 + 등식** (부등식이 핵심 제약으로 등장하는 것은 이전 4회 시도에 없음)
- **수학적 객체:** 삼차함수 $f$ 단독 → **삼차함수 $f$ + 적분함수 $g$** (적분으로 정의된 새 함수가 조건의 주체)
- **숨은 사실의 표현:** 직접적(극솟값=0, 접선 재교점 등) → **부등식으로 포장** (완전제곱 강제가 두 조건의 교차에서만 출현)
- **발문 유형:** 동일(수치 계산) 유지 — 핵심 난도는 조건 해석에 집중

---

## [무대 설정]

최고차항의 계수가 $1$이고 $f(0)=0$인 삼차함수 $f(x)$와, 이를 피적분함수로 하는 적분함수 $g(x)=\int_0^x f(t)\,dt$를 무대로 삼는다. 미결 파라미터는 이차계수 $a$, 일차계수 $b$, 그리고 영점 위치 $\alpha$(양수).

---

## [조건 배치 계획]

| 수학적 상황 요소 | 번역 대상 | 설계 의도 |
|---|---|---|
| $f(x) = x^3+ax^2+bx$, $f(0)=0$ | **문제 설정**(도입부) | 파라미터 $(a,b)$를 암묵적으로 도입 |
| 제약 A: $H(x)\ge 0\;(\forall x>0)$ | **(가)** 부등식 조건 | 단독으로는 $(a,b)$의 **영역**만 제한 — 무해해 보임 |
| 제약 B: $H(\alpha)=0$ | **(나)** 등식 조건 | 단독으로는 $b$를 $a$로 표현할 뿐 — 자유도 1이 남아 결정 불가로 보임 |
| 숨은 사실: (가)의 영역과 (나)의 직선이 포물선에 접하여 $(a,b)$ 유일 결정 | 조건 (가)+(나) 연립 시 출현 | 수험생이 대입 후 완전제곱 부등식 $(2a+9)^2\le 0$을 **스스로** 발견해야 함 |

**숨김 점검:**
- **(가)** 만 보면: "$g$가 양수에서 비음수"라는 자연스러운 성질 — $a,b$에 대한 판별식 조건으로 귀결된다는 사실은 보이지 않음.
- **(나)** 만 보면: 정적분 $= 0$이라는 평범한 조건 — 이것이 (가)와 맞물려 자유도를 완전히 소진한다는 사실은 보이지 않음.
- **연립할 때만:** (나)의 등식을 (가)의 부등식에 대입하면 완전제곱 $\le 0$이 나타나, 등호가 강제된다.

---

## [문제 초안]

> 최고차항의 계수가 $1$이고 $f(0) = 0$인 삼차함수 $f(x)$에 대하여
>
> $$g(x) = \int_0^x f(t)\,dt$$
>
> 라 하자. 함수 $g(x)$가 다음 조건을 만족시킨다.
>
> **(가)** 모든 양수 $x$에 대하여 $g(x) \ge 0$이다.
>
> **(나)** $g(3) = 0$
>
> $f(5)$의 값을 구하시오.

---

## [조건-상황 대응표]

| 조건 | 수학적 상황의 요소 | 역할 |
|---|---|---|
| 도입부 "$f(0)=0$인 삼차함수" | $f(x)=x^3+ax^2+bx$ | 상수항 제거, 파라미터 $(a,b)$ 도입 |
| 도입부 "$g(x)=\int_0^x f(t)\,dt$" | 적분함수 $H(x)$ | $g(x)=\tfrac{x^2}{4}\bigl(x^2+\tfrac{4a}{3}x+2b\bigr)$ 구조를 은폐 |
| **(가)** $g(x)\ge 0\;(\forall x>0)$ | 제약 A — 누적 적분의 비음수성 | $(a,b)$에 대한 2차원 영역(포물선 위쪽) 부과 |
| **(나)** $g(3)=0$ | 제약 B — 특정 점에서 적분값 영 | $(a,b)$ 사이 선형 관계 $9+4a+2b=0$ 부과 |
| 발문 $f(5)$ | 강제 결과 $f(x)=x(x-\tfrac{3}{2})(x-3)$에서 함수값 | 유일 결정된 $f$의 확인 |