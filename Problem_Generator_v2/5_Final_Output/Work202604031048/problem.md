# 생성된 문제
> 2026-04-03T11-08-16

---

## 1. 아이디어 셀렉터 (Idea Maker)

### 수학적 상황
**수학적 객체**: 세 서로 다른 실근을 갖는 모닉 삼차다항함수 $f(x) = x^3 + ax^2 + bx + c$.

**사실 A가 부과하는 제약 (P001)**:
자기합성 방정식 $f(x - f(x)) = 0$의 근의 총 개수가 특정 값으로 주어진다. 이 방정식은 $f(x) = x - r_i$ ($i = 1, 2, 3$)인 세 분기로 분해되며, 모든 분기가 기울기 1인 평행선과의 교점 문제이므로 분기 간 해가 겹치지 않는다. 각 근 $r_i$는 자동으로 해이고, 분기에서 접선(이중근)이 되려면 $f'(r_i) = 1$이어야 한다. 총 근의 개수 제약이 **정확히 2개의 분기에서 접선이 발생할 것**을 강제한다.

**사실 B가 부과하는 제약 (P011)**:
삼차식의 세 근에서 평가한 도함수값의 합은 $f'(r_1) + f'(r_2) + f'(r_3) = a^2 - 3b$라는 항등식을 만족한다. 이는 근의 기본 대칭함수만으로 결정되는 고정된 값이다.

**숨은 사실 (A+B 동시 작용)**:
$f'(x) - 1$은 이차식이므로 $f'(r_i) = 1$을 만족하는 근은 최대 2개이다. 2개의 접선 분기를 강제하면, 접선 조건을 갖는 두 근 $r_1, r_2$는 이차방정식 $f'(x) = 1$의 근이 되어 $r_1 + r_2 = -2a/3$을 만족한다. 비에타 공식 $r_1 + r_2 + r_3 = -a$와 결합하면 $r_3 = -a/3$, 이는 정확히 $f$의 **변곡점**의 $x$-좌표이다. 따라서 삼차함수의 변곡점이 반드시 $x$-축 위에 놓이며, 이로 인해 삼차함수의 점대칭 중심이 근이 되는 구조가 강제되어 계수 $c$가 $a, b$의 함수로 확정된다.

---

### 수험생의 발견 순간
1. **수험생이 처음 시도하는 것**: $f(x - f(x)) = 0$을 전개하려 하지만 9차식이 되어 막막하다. $f(t) = 0$의 근 $r_i$를 이용해 $f(x) = x - r_i$ 세 분기로 분해하고, 각 분기의 근의 개수를 세기 시작한다. 각 $r_i$가 자동 해임을 확인하고, 나머지는 이차방정식의 판별식으로 분류한다.

2. **막히는 순간**: 총 근의 개수 조건을 맞추려면 "일부 분기에서 접선(이중근)이 필요하다"는 것까지는 도달하지만, $f'(r_i) = 1$ 조건이 최대 몇 개의 근에서 성립할 수 있는지, 그리고 접선이 되지 않는 나머지 근에서 어떤 일이 일어나는지를 연결하지 못한다.

3. **발견**: "$f'(x) - 1$은 이차식이니까 $f'(r_i) = 1$인 근은 최대 2개밖에 안 되잖아!" — 이 순간, 접선 조건이 2개인 두 근의 합을 이차방정식의 근과 계수 관계로 구하고, 비에타 공식과 합산하면 **세 번째 근이 $-a/3$, 즉 변곡점의 위치로 강제됨**을 발견한다. 변곡점이 근이라는 기하적 조건이 $c$를 결정하여 문제가 풀린다.

4. **숨은 사실과의 대응**: 이 발견은 STEP 2-2의 "접선 분기의 최대화 → 나머지 근이 변곡점에 고정"이라는 숨은 사실과 정확히 대응한다. 수험생은 "자기합성의 분기 구조"(조합론적)와 "도함수의 차수 제한"(대수적)이라는 두 독립적 제약이 만나 "변곡점이 근"이라는 기하적 결론을 만들어내는 순간을 경험한다.

---

## 2. 프라블럼 메이커 (Problem Maker)

### 문제 초안
> **최고차항의 계수가 $1$인 삼차함수 $f(x)$가 다음 조건을 만족시킨다.**
>
> **(가)** 방정식 $f(x) = 0$은 서로 다른 세 실근을 가지며, 세 근의 합은 $s$이다.
>
> **(나)** 방정식 $f\!\bigl(x - f(x)\bigr) = 0$의 서로 다른 실근의 개수는 $N$이다.
>
> **(다)** $f(x)$의 극댓값과 극솟값의 차는 $M$이다.
>
> $f(t)$의 값을 구하시오.

*(여기서 $s,\; N,\; M,\; t$는 수치 결정 단계에서 확정할 정수 또는 유리수 상수이다.)*

---

##

### 조건-상황 대응표
| 조건 | 담당하는 수학적 상황 요소 | 수험생이 얻는 정보 |
|---|---|---|
| **(가)** 세 실근 존재 + 합 $= s$ | 삼차다항식의 실근 구조 + 비에타 제1관계 | $a = -s$ (계수 $a$ 확정) |
| **(나)** $f(x-f(x))=0$의 실근 개수 $= N$ | 사실 A: 자기합성 방정식의 분기 구조와 접선 개수 제약 | 분기 해석 → 2개의 접선 분기 강제 → **숨은 사실 발동** ($c$가 $a,b$의 함수로 확정) |
| **(다)** 극댓값 $-$ 극솟값 $= M$ | 사실 B: $a^2 - 3b$ 결정 (극값 차 공식을 통한 간접 제공) | $(a^2 - 3b)^{3/2} = \frac{27M}{4}$ → $b$ 확정 |
| **발문** $f(t) = \;?$ | 세 계수 $a,b,c$가 모두 필요한 질문 | 숨은 사실 없이는 $c$를 결정할 수 없어 답을 낼 수 없음 |

---

## 3. 디테일 빌더 (Detail Builder)

### 완성된 문제
> **최고차항의 계수가 $1$인 삼차함수 $f(x)$가 다음 조건을 만족시킨다.**
>
> **(가)** 방정식 $f(x) = 0$은 서로 다른 세 실근을 가지며, 세 근의 합은 $3$이다.
>
> **(나)** 방정식 $f\!\bigl(x - f(x)\bigr) = 0$의 서로 다른 실근의 개수는 $7$이다.
>
> **(다)** $f(x)$의 극댓값과 극솟값의 차는 $\dfrac{\sqrt{6}}{9}$이다.
>
> $f(5)$의 값을 구하시오. **[4점]**

$$\boxed{62}$$

---

## 4. 이벨류에이터 (Evaluator)

### 원본 문제 A
## Problem

**1.** 2022학년도 6월 평가원

삼차함수 $f(x)$가 다음 조건을 만족시킨다.

> (가) 방정식 $f(x) = 0$의 서로 다른 실근의 개수는 2이다.
> (나) 방정식 $f(x - f(x)) = 0$의 서로 다른 실근의 개수는 3이다.

$f(1) = 4$, $f'(1) = 1$, $f'(0) > 1$일 때, $f(0) = \frac{q}{p}$이다. $p + q$의 값을 구하시오. (단, $p$와 $q$는 서로소인 자연수이다.)

### 원본 문제 B
## Problem

**11.** 두 상수 $a$, $b$ ($b < 0$)에 대하여 함수

$$f(x) = \begin{cases} x^3 + 4x^2 + b & (x < 0) \\ x^3 - ax^2 + ax + b & (x \geq 0) \end{cases}$$

이 다음 조건을 만족시킬 때, $27(a+b)$의 값을 구하시오.

> (가) 방정식 $f(x) = 0$의 모든 실근을 나열하면
> $\alpha$, $\beta$, $\gamma$ ($\alpha < \beta < \gamma$)이고 $\alpha + \beta + \gamma = -4$이다.
> (나) $f'(\alpha) + f'(\beta) + f'(\gamma) = 14$

### 채점
| 항목 | 점수 | 근거 (STEP 1 참조) |
|------|------|-------------------|
| 융합성 | 5/5 | 1-1: 세 조건 모두 단독으로는 미결 파라미터가 남음. (가)→$a$, (가)+(다)→$b$, (가)+(나)→$c$ 구조로, 특히 (나)와 (다)가 각각 $c$와 $b$를 담당하면서도, (나)의 해석이 "변곡점이 근"이라는 예상치 못한 구조를 드러냄. 순차 구조가 아니라 (나)의 접선 조건과 비에타 공식의 **비자명한 결합**이 핵심. |
| 참신함 | 4/5 | 원본 A와 같은 자기합성 방정식 $f(x-f(x))=0$ 구조를 사용하나, 원본 A는 이중근+도함수 조건이 주어진 반면, 본 문제는 세 실근+극값 차를 통해 간접적으로 구조를 강제함. 풀이 전략이 실질적으로 다름: 원본 A는 $f(1)=4,\,f'(1)=1$로 직접 접선 조건을 부여하지만, 본 문제는 극값 차에서 $b$를 구한 후 접선 조건의 **차수 제한**을 통해 $c$를 발견하는 경로. 원본 B와는 객체·발문이 완전히 다름. |
| 정합성 | 5/5 | 1-2: 풀이 전 과정이 고교 범위 내(인수분해, 비에타, 판별식, 극값 차 공식). 조건 서술이 "서로 다른 세 실근", "극댓값과 극솟값의 차" 등 수능 표준 용어를 사용. 형식(4점 주관식, 조건 (가)(나)(다) 배치)도 수능 킬러 문항 형식에 정확히 부합. |
| 논리성 | 5/5 | 1-3: 해가 유일함을 확인(복소근·판별식영점 시나리오 모두 배제). 1-4: 세 조건 각각 제거 시 답 결정 불가 — 모든 조건이 필수적이며 과잉·부족 없음. |
| 풀이 깊이 | 5/5 | 1-2: 비자명한 전환 2회 (①모든 $D_i>0$ 확인→접선 2개 필요, ②$f'(x)-1$의 차수 제한→비에타→변곡점=근). 설계된 발견 지점("$f'(x)-1$은 이차식이니까 최대 2개!")이 풀이에서 실제로 핵심 역할을 담당. 단순 계산이 아닌 구조적 통찰이 요구됨. |
| **총점** | **24/25** | |

---

##