[THINKING]
Let me carefully analyze the PM's structure and complete this problem.

## Phase 1: Structure Analysis

The PM gives us:
- $f(x) = x^3 + ax^2 + bx + c$ (leading coefficient 1, cubic)
- Condition (가): The tangent line at the inflection point passes through the origin
- Condition (나): The local minimum value of $f(x)$ is $0$
- Condition (다): $f(0) = k$ (value to be determined)
- Question: Find the local maximum value

Let me work through the math.


The inflection point occurs where $f''(x) = 0$, which gives $x = -a/3$. At this point, I need to find both the function value and the slope of the tangent line to use the condition that it passes through the origin.

Now I'm setting up the tangent line equation at the inflection point and applying the constraint that it passes through $(0,0)$. This gives me a relationship between the coefficients: after substituting and simplifying, I get $c = a^3/27$.

From condition (다), since $f(0) = c = k$, I have $a^3/27 = k$, which means $a = 3k^{1/3}$. So $a$ is completely determined by $k$, but $b$ remains a free parameter.

For condition (나), I need the local minimum value to be $0$. The derivative $f'(x) = 3x^2 + 2ax + b$ must have two distinct real roots for local extrema to exist, requiring $b < a^2/3$. I'm identifying the critical points where $x_1$ is the local minimum.

Since $f(x_2) = 0$ at the local minimum and $f'(x_2) = 0$, this means $x_2$ is a double root of $f$. So I can write $f(x) = (x - x_2)^2(x - r)$ for some value $r$, and I'm expanding this to match the original cubic form.

Comparing coefficients with $f(x) = x^3 + ax^2 + bx + c$, I get relationships between $a$, $b$, $c$ and the roots. Using the constraint that $c = a^3/27$, I can set up an equation to solve for the roots in terms of the coefficients. Let me denote the double root as $s$ and the simple root as $t$ to simplify the algebra.

With $a = -(2s + t)$, $b = s^2 + 2st$, and $c = -s^2 t$, the constraint becomes $27s^2 t = (2s+t)^3$. Now I'll introduce a ratio $\lambda = t/s$ to reduce this to a single variable equation: $27\lambda = (2+\lambda)^3$. Substituting $v = 2 + \lambda$ gives me a cleaner form to solve for the relationship between the roots.

Testing $v = 3$ in the resulting cubic confirms it's a root, and factoring reveals $(v-3)^2(v+6) = 0$, giving $v = 3$ as a double root or $v = -6$. When $v = 3$, I get $\lambda = 1$ and $t = s$, which means the polynomial becomes $(x-s)^3$—a triple root case.

The derivative $f'(x) = 3(x-s)^2$ has a double root at $x = s$, creating an inflection point rather than a local minimum, which contradicts the given condition.

For $v = -6$, I have $\lambda = -8$ and $t = -8s$, so I'm calculating the coefficients: $a = 6s$, $b = -15s^2$, and $c = 8s^3$.

Now I'm verifying this works by expanding the factored form $(x-s)^2(x+8s)$ and checking it matches $x^3 + 6sx^2 - 15s^2x + 8s^3$.

For the critical points, I'm taking the derivative to get $f'(x) = 3(x+5s)(x-s)$, which gives critical points at $x = -5s$ and $x = s$. When $s > 0$, the local minimum occurs at $x = s$ where $f(s) = 0$, and when $s < 0$, the behavior of these critical points reverses.

Evaluating at $x = -5s$: I get $f(-5s) = 108s^3$. If $s < 0$, this would be negative, which contradicts what I need. So $s$ must be positive.

With $s > 0$, the local maximum is at $x = -5s$ with value $108s^3$, and from the condition $f(0) = 8s^3 = k$, I can relate the parameters.

The local maximum value becomes $\frac{27k}{2}$, which matches the expected ratio. For this to be an integer, $k$ needs to be even. Testing $k = 2$ gives an answer of $27$, though the resulting value of $s$ isn't particularly clean.

With $k = 8$, I get an answer of $108$ and $s = 1$, which is much nicer. This gives me the polynomial $f(x) = x^3 + 6x^2 - 15x + 8$ with coefficients $a = 6$, $b = -15$, $c = 8$. The inflection point is at $x = -2$, and I'm calculating $f(-2)$ to verify the value there. The local minimum at $x = 1$ gives $f(1) = 0$, and the local maximum at $x = -5$ yields $f(-5) = 108$, with $f(0) = 8$ confirming the setup. So $k = 8$ with $s = 1$ produces the cleanest answer of $108$ with all integer coefficients. I should verify that $s$ must be positive since we need the local minimum to equal zero, which rules out $k = -8$, and checking other values like $s = 2$ doesn't yield anything better. I'm checking which candidate works best. The first option with $k = 8$ gives me a polynomial with all integer coefficients that factors nicely as $(x-1)^2(x+8)$, and it's the only solution where $s > 0$ works cleanly. The other candidates either involve irrational values for $s$ or produce messier intermediate calculations, even though they yield smaller answers.

So Candidate 1 is clearly the best choice. Now I'm framing this as a proper exam problem with three conditions: the tangent at the inflection point passes through the origin, the local minimum equals zero, and $f(0) = 8$. The question asks for the local maximum. From the first condition I get $c = a^3/27$, and combining with the third condition gives $a = 6$, which uniquely determines everything.

With $a = 6$ fixed, I need to find $b$ using the local minimum condition. Since $f'(x) = 3x^2 + 12x + b$, for critical points to exist I need $b < 12$. At the local minimum point $x_0$, both $f'(x_0) = 0$ and $f(x_0) = 0$ must hold, which gives me two equations to solve for $b$ and $x_0$.

Substituting the expression for $b$ from the derivative condition into the function equation, I get a cubic in $x_0$: $x_0^3 + 3x_0^2 - 4 = 0$. Testing $x_0 = 1$ works, so I can factor this as $(x_0 - 1)$ times a quadratic.

Completing the factorization gives $(x_0 - 1)(x_0 + 2)^2 = 0$, so the critical points are $x_0 = 1$ and $x_0 = -2$.

For $x_0 = 1$, I get $b = -15$, making $f(x) = x^3 + 6x^2 - 15x + 8$. The derivative factors as $3(x+5)(x-1)$, giving local extrema at $x = -5$ and $x = 1$. Since $f(1) = 0$, the local minimum is indeed zero, and now I need to check the local maximum value at $x = -5$.

When $x_0 = -2$, I get $b = 12$, so $f(x) = (x+2)^3$. But this has no local extrema since the derivative is always non-negative, which violates the second condition.

Therefore $b = -15$ is the unique solution, and the answer is 108. I'm also thinking about how elegant this problem is—the cubic equation factors perfectly to give exactly one valid case, and all three conditions work together to pin down the answer uniquely. aneous $f = 0$ and $f' = 0$), solving the resulting cubic and factoring out the triple root case, then computing the local maximum value. This is definitely a solid 4-point problem with good depth.

For the 수능 format, I'll present it as a 주관식 worth 4 points with answer 108. The condition order (가) → (나) → (다) flows naturally—starting with the structural inflection tangent relationship, moving to the local minimum constraint, and finishing with the scale-determining value at zero. Let me verify this ordering works cleanly.

Now I'm checking whether the local minimum or maximum could be zero instead. For a cubic with positive leading coefficient, the first critical point is the local maximum and the second is the local minimum, so "극솟값은 0" correctly refers to the local minimum at $x_2 = 1$, giving $f(1) = 0$. This creates the double root structure the problem requires—since the cubic goes from $-\infty$ to $+\infty$, having the local minimum equal zero forces a double root there. I need to find the critical points by taking the derivative. Computing $f'(x)$ gives me $(x-s)(3x - 2t - s)$, so the critical points are at $x = s$ and $x = \frac{2t+s}{3}$. For $s$ to actually be a local minimum rather than an inflection point, I need these to be distinct, meaning $t \neq s$.

Using the second derivative test, $f''(s) = 2(s-t)$, which is positive when $t < s$. With $s = 1$ and $t = -8$, this condition holds. The other critical point lands at $x = -5$, which all checks out.

Now I'm verifying the PM's factorization claim about $(3u+1)^2(6u-1) = 0$ where $u = r/a$ represents the double root position.

Substituting $u = 1/6$ into $54u^3 + 27u^2 - 1$ gives $1/4 + 3/4 - 1 = 0$, and checking $u = -1/3$ also yields zero. The factorization appears to be $(3u+1)^2(6u-1)$ based on these roots.

Let me verify this factorization expands correctly: multiplying out $(3u+1)^2(6u-1)$ gives $54u^3 + 27u^2 - 1$, confirming the PM's analysis is sound.

Looking at the problem's difficulty, it requires recognizing that a local minimum at zero implies a double root, setting up the resulting cubic equation, and working through the algebra—a solid problem that combines computational and conceptual skills.

Now I'm double-checking the key factorization $x_0^3 + 3x_0^2 - 4 = (x_0-1)(x_0+2)^2$, and expanding it confirms this is correct. The solution is ready to present.

Working through the conditions: the inflection point constraint gives me $c = a^3/27$, and since $f(0) = 8$, I get $a = 6$. For the local minimum condition, I need to find where $f'(x_0) = 0$ and $f(x_0) = 0$ simultaneously, which determines $b$ in terms of $x_0$.

Substituting the derivative condition into the function value equation and simplifying, I arrive at a cubic that factors as $(x_0 - 1)(x_0 + 2)^2 = 0$, giving $x_0 = 1$ or $x_0 = -2$.

Testing $x_0 = -2$ yields $b = 12$ and $f(x) = (x+2)^3$, which has no local extrema—a contradiction. So $x_0 = 1$ must hold, giving $b = -15$ and $f(x) = x^3 + 6x^2 - 15x + 8$. The derivative factors as $3(x+5)(x-1)$, placing a local maximum at $x = -5$ where $f(-5) = 108$.

Now I'm thinking about how to phrase the conditions clearly for a 수능-style problem. The standard form would be to state that the local minimum value is 0, using the conventional phrasing "함수 $f(x)$의 극솟값은 $0$이다." I should also verify that 108 is a reasonable answer magnitude for this type of problem, and it definitely is.

---

[OUTPUT]
# [STEP 0] 피드백 반영 선언

| 이전 실패 패턴 | 이번 회피 전략 |
|---|---|
| Draft 1: (나) $xf(x)\ge0$이 풀이 과정에서 순차 필터(소거용)에 불과 → 풀이깊이 2점 | (나) 극솟값 $=0$이 $f=0$, $f'=0$ 연립을 강제하여 $x_0$에 대한 **3차 방정식**을 생성하므로, 단순 소거가 아닌 **분기 해석** 역할 수행 |
| Draft 2: 조건 설계 미완성으로 중도 탈락, (나)의 절댓값·개수 조건이 PM 핵심(변곡점 접선·이중근)과 무관 | 이번 PM이 제시한 수학적 상황(변곡점 접선·이중근 구조)을 조건 문면에 **직접** 반영 |
| 두 초안 모두 발문이 단순 대입 ($f(k)$) → 구조 파악 없이 산출 가능 | 발문을 **극댓값**으로 설정하여, 분기 선택 → 극점 위치 결정 → 함수값 계산의 다단계 추론 요구 |

---

# [STEP 1] 구조 분석

## 1-1. 조건이 만드는 방정식 체계

$f(x) = x^3 + ax^2 + bx + c$. 자유 파라미터: $(a,\,b,\,c)$ — 3개.

**(가) 변곡점 접선이 원점 통과**

변곡점: $x = -\dfrac{a}{3}$.

$$f\!\left(-\tfrac{a}{3}\right) = \tfrac{2a^3}{27} - \tfrac{ab}{3} + c, \qquad f'\!\left(-\tfrac{a}{3}\right) = b - \tfrac{a^2}{3}$$

접선 $y = f'(-\tfrac{a}{3})\,(x + \tfrac{a}{3}) + f(-\tfrac{a}{3})$이 원점을 지나려면:

$$0 = \left(b - \tfrac{a^2}{3}\right)\!\cdot\tfrac{a}{3} + \tfrac{2a^3}{27} - \tfrac{ab}{3} + c$$

정리하면 $\;\boxed{c = \dfrac{a^3}{27}}$ … ①. 자유도: $(a,\,b)$ — 2개.

**(나) 극솟값 $= 0$**

극소점 $x_0$에서 $f'(x_0)=0$이고 $f(x_0)=0$이 동시에 성립. 
$x_0$가 이중근이므로 $f(x) = (x - x_0)^2(x - t)$로 표현 가능. 이로부터:

- $a = -(2x_0 + t)$
- $b = x_0^2 + 2x_0 t$
- $c = -x_0^2\, t$

①과 결합하면 $b$가 $(a,\, x_0)$에 종속 → 자유도 감소. 그러나 $a$가 미결이므로 여전히 1자유도.

**(다) $f(0) = k$**

$c = k$와 ①을 결합: $\dfrac{a^3}{27} = k$ → $a = 3\sqrt[3]{k}$. 스케일 고정 → 자유도 0.

## 1-2. 분기 분석

$c = \dfrac{a^3}{27}$과 $c = -x_0^2\,t$를 연립하면, $t = x_0\lambda$로 놓을 때:

$$27\lambda = (2 + \lambda)^3$$

$v = 2 + \lambda$으로 치환: $v^3 - 27v + 54 = 0$.

인수분해: $(v - 3)^2(v + 6) = 0$.

| 분기 | $v$ | $\lambda$ | $t$ | 결과 |
|---|---|---|---|---|
| **Case A** | $v = 3$ | $\lambda = 1$ | $t = x_0$ | $f(x) = (x-x_0)^3$: 삼중근 → 극값 없음 → **(나)에 모순** |
| **Case B** | $v = -6$ | $\lambda = -8$ | $t = -8x_0$ | 이중근 $x_0$, 단근 $-8x_0$ → 극소·극대 존재 → **유효** |

**Case B**: $a = -(2x_0 + (-8x_0)) = 6x_0$, $\;b = x_0^2 - 16x_0^2 = -15x_0^2$.

극소점이 존재하려면 $f''(x_0) = 2(x_0 - t) = 2(x_0 + 8x_0) = 18x_0 > 0$ → $x_0 > 0$.

### 판정 1-2
Case A 자동 탈락, Case B 유일 생존. **CONTINUE**.

## 1-3. 자유도 계산

| 조건 | 소거 대상 | 잔여 자유도 |
|---|---|---|
| 초기 | — | $(a,\,b,\,c)$: 3 |
| (가) | $c = a^3/27$ | 2 |
| (나) | $b = -15x_0^2$, $a = 6x_0$ → $b = -5a^2/12$ | 1 ($a$만 잔여) |
| (다) | $a^3/27 = k$ → $a$ 확정 | **0** |

### 판정 1-3
자유도 $= 0$. 해 유일. **CONTINUE**.

## 1-4. 탐색 전략 수립

- $k$는 $a^3/27$이 되어야 하므로, $a$가 정수이면 $k$ 정수.
- 극댓값 $= 108\,x_0^3 = 108\cdot\dfrac{a^3}{216} = \dfrac{a^3}{2} = \dfrac{27k}{2}$.
- 답이 정수이려면 $k$는 짝수. **$k = 2$이면 답 $27$, $k = 8$이면 답 $108$.**
- $k = 8$ → $a = 6$, $x_0 = 1$: 모든 계수가 정수. 풀이 중간 3차 방정식 $(x_0 - 1)(x_0 + 2)^2 = 0$으로 깔끔한 인수분해. **CONTINUE**.

---

# [STEP 2] 수치 탐색 + 포장 설계

## 후보 탐색

| 후보 | $k$ | $a$ | $x_0$ | $b$ | 계수 | 답 | 3차 인수분해 | 판정 |
|---|---|---|---|---|---|---|---|---|
| ① | $8$ | $6$ | $1$ | $-15$ | 모두 정수 | $108$ | $(x_0-1)(x_0+2)^2=0$ ✓ | **채택** |
| ② | $2$ | $3\sqrt[3]{2}$ | — | — | 무리수 | $27$ | 중간 과정 비정수 | 기각 |
| ③ | $4$ | $3\sqrt[3]{4}$ | — | — | 무리수 | $54$ | 중간 과정 비정수 | 기각 |
| ④ | $16$ | $12$ | $2$ | $-60$ | 정수 | $432$ | $(x_0-2)(x_0+4)^2=0$ | 답 과대 |

## 채택: 후보 ①

**수치**: $f(x) = x^3 + 6x^2 - 15x + 8 = (x-1)^2(x+8)$

**검증**:
- 변곡점 $x = -2$: $f(-2) = -8 + 24 + 30 + 8 = 54$, $\;f'(-2) = 12 - 24 - 15 = -27$
  접선: $y = -27(x+2) + 54 = -27x$. 원점 통과 ✓
- 극소: $f'(x) = 3(x+5)(x-1)$. 극소점 $x=1$: $f(1) = 0$ ✓
- 극대: $x = -5$: $f(-5) = -125 + 150 + 75 + 8 = 108$ ✓
- $f(0) = 8$ ✓

**유일성**: 3차 방정식 $(x_0-1)(x_0+2)^2 = 0$에서 $x_0=1$만 극소조건 충족. 해 유일 ✓

**손계산 확인**: 모든 계수 정수, 인수분해 $(x_0-1)(x_0+2)^2$은 $x_0=1$ 대입으로 발견 가능. $f(-5)$ 계산도 직접 산술. ✓

## 포장 설계

| 항목 | 설계 | 근거 |
|---|---|---|
| **발문** | 극댓값 구하기 | 분기 선택 이후 극대점 위치($x=-5$)까지 결정해야 산출 가능 |
| **조건 순서** | (가) 변곡점 접선 → (나) 극솟값 → (다) $f(0)$ | 구조적 조건 → 핵심 제약 → 스케일 고정의 자연스러운 흐름 |
| **수치 은닉** | $a=6$, $b=-15$는 노출하지 않음 | 학생이 (가)→(다) 순서로 $a$를 먼저 결정할지, (나)와 동시에 공략할지 전략적 선택 필요 |
| **배점** | 4점 (주관식) | 3단계 추론(①변곡점 접선 조건, ②이중근 인식+3차 방정식, ③분기 제거)이 요구됨 |

---

# [STEP 3] 최종 검증 + 완성된 문제

## 검증

| 항목 | O/X | 비고 |
|---|---|---|
| 모든 조건 필요 | O | (가) 없으면 $c$ 미결, (나) 없으면 $b$ 미결, (다) 없으면 $a$ 미결 |
| 불필요한 조건 없음 | O | 3개 조건으로 3개 파라미터 정확히 결정 |
| 수능 어투 | O | 표준 서술 |
| 발문 자연스러움 | O | "극댓값을 구하시오" — 수능 기출 발문 형태 |
| PM 상황 보존 | O | 변곡점 접선·이중근 구조·분기 $(3u+1)^2(6u-1)=0$ 모두 유지 |

## 포장 변경 이력

| PM 원안 | 최종 포장 | 변경 사유 |
|---|---|---|
| (다) $f(0) = k$ (미정) | (다) $f(0) = 8$ | $k=8$ → $a=6$, 모든 계수 정수, 풀이 중 3차 방정식 $(x_0-1)(x_0+2)^2=0$으로 깔끔한 인수분해 |
| 발문: 극댓값 (PM 제안) | 극댓값 (유지) | PM 의도 그대로 채택 |

---

## 완성된 문제

$$\boxed{\text{[주관식]}}$$

> 최고차항의 계수가 $1$인 삼차함수 $f(x)$가 다음 조건을 만족시킨다.
>
> **(가)** 곡선 $y = f(x)$의 변곡점에서의 접선이 원점을 지난다.
>
> **(나)** 함수 $f(x)$의 극솟값은 $0$이다.
>
> **(다)** $f(0) = 8$
>
> $f(x)$의 극댓값을 구하시오. $\qquad$ **[4점]**

$$\boxed{108}$$