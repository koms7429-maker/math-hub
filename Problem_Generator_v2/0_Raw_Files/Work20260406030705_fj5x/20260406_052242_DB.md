[THINKING]
Let me carefully analyze this problem structure and complete all phases without truncation.

## Understanding the PM's structure:

We have:
- $f(x) = x^3 + ax^2 + bx$ (cubic with leading coefficient 1, $f(0)=0$)
- $g(x) = \int_0^x f(t)\,dt$
- (가) $g(x) \ge 0$ for all $x > 0$
- (나) $g(3) = 0$
- Find $f(5)$
  
Now I'm computing the integral to get $g(x) = \frac{x^4}{4} + \frac{ax^3}{3} + \frac{bx^2}{2}$, which I can factor as $g(x) = \frac{x^2}{2}\left(\frac{x^2}{2} + \frac{2ax}{3} + b\right)$.

Let me refactor this more carefully by pulling out $\frac{x^2}{12}$ to get $g(x) = \frac{x^2}{12}(3x^2 + 4ax + 6b)$. Since the first factor is always positive for $x > 0$, the condition that $g(x) \ge 0$ reduces to requiring $Q(x) = 3x^2 + 4ax + 6b \ge 0$ for all positive $x$. With a positive leading coefficient, this either means the discriminant is non-positive, giving $16a^2 - 72b \le 0$, or the roots are both non-positive. For both roots to be non-positive, I need the sum of roots to be at most zero (requiring $a \ge 0$) and the product to be non-negative (requiring $b \ge 0$). So the condition from this case becomes: either $b \ge \frac{2a^2}{9}$, or both $a \ge 0$ and $b \ge 0$ with a positive discriminant. But when $a \ge 0$ and $b \ge 0$, the quadratic is automatically positive for all $x > 0$ since every term is non-negative.

Now I'm reconsidering the overall constraint more carefully—I need $Q(x) \ge 0$ for all positive $x$.

Since $Q$ is an upward-opening parabola, it violates this constraint exactly when it has a positive root. The larger root $\frac{-4a + \sqrt{16a^2 - 72b}}{6}$ is positive when either $a < 0$ or when $a \ge 0$ and $b < 0$.

So condition (가) requires either $b \ge \frac{2a^2}{9}$ or both $a \ge 0$ and $b \ge 0$.

Now I'm moving to condition (나), which states $g(3) = 0$, and I need to evaluate what that expression equals.

Computing $g(3)$, I get $\frac{9}{4}(9 + 4a + 2b) = 0$, which means $b = -\frac{9+4a}{2}$.

Substituting this relationship back into condition (가), I notice that $b \ge 0$ only when $a \le -\frac{9}{4}$, so if $a \ge 0$, then $b$ must be negative, which rules out the second part of condition (가).

Now I need to verify whether the first part of condition (가)—that $b \ge \frac{2a^2}{9}$—is satisfied with this substitution.

Working through the algebra, I get $(2a + 9)^2 \le 0$, which forces $a = -\frac{9}{2}$ and $b = \frac{9}{2}$.

Now I need to check the other cases. If $a \ge 0$ and $b \ge 0$, then with $b = -\frac{9+4a}{2}$, we'd have $b < 0$, so that's impossible. For $a < 0$ and $b \ge 0$ (meaning $a \le -\frac{9}{4}$), I need to verify whether $Q(x) = 3x^2 + 4ax + 6b$ stays non-negative for all $x > 0$. Since $a < 0$, the vertex is at $x = -\frac{2a}{3} > 0$, and I need to check if the minimum value is non-negative.

The minimum value at the vertex is $6b - \frac{4a^2}{3}$, so I need $b \ge \frac{2a^2}{9}$ for $Q(x) \ge 0$ everywhere, which also ensures $Q(x) \ge 0$ for all $x > 0$ since the vertex lies in that region. But I should reconsider the full condition—it seems like the requirement is either $b \ge \frac{2a^2}{9}$ or the case where both $a \ge 0$ and $b \ge 0$.

When $a \ge 0$, the vertex sits at $x = -\frac{2a}{3} \le 0$, so for positive $x$ the parabola is increasing. Since $Q(0) = 6b$, I need $Q(x) \ge 0$ for all $x > 0$, which means $6b \ge 0$, so $b \ge 0$ is sufficient in this case.

When $a < 0$, the vertex moves to $x_0 = -\frac{2a}{3} > 0$, and the minimum value of $Q$ on the positive side is $Q(x_0) = 6b - \frac{4a^2}{3}$. For this to be non-negative, I need $b \ge \frac{2a^2}{9}$.

So the condition splits: $b \ge 0$ when $a \ge 0$, and $b \ge \frac{2a^2}{9}$ when $a < 0$.

Now I'm checking whether these conditions can be unified. When $a \ge 0$, the bound $\frac{2a^2}{9}$ is always non-negative, so $b \ge \frac{2a^2}{9}$ would be stronger than $b \ge 0$. But the actual requirement is just $b \ge 0$ in this case, so I can't simply use $b \ge \frac{2a^2}{9}$ for all $a$.

Testing the boundary case where $b = -\frac{9+4a}{2}$: when $a \ge 0$, this gives $b < 0$, which violates the condition. When $a < 0$, I need $b \ge \frac{2a^2}{9}$, which leads to $(2a+9)^2 \le 0$, forcing $a = -\frac{9}{2}$.

So the unique solution is $a = -\frac{9}{2}$ and $b = \frac{9}{2}$. With these values, $f(x) = x^3 - \frac{9}{2}x^2 + \frac{9}{2}x$ factors as $x(x - \frac{3}{2})(x - 3)$ after finding the roots of the quadratic factor. Now I'm evaluating $f(5)$ using this factorization.

Computing $f(5) = 5 \cdot \frac{7}{2} \cdot 2 = 35$.

Let me verify this works by checking $g(x)$. I can rewrite it as $\frac{x^2(x-3)^2}{4}$, which is always non-negative and equals zero only at $x = 0$ and $x = 3$. This confirms the conditions are satisfied—the problem elegantly forces $g(x)$ to be a perfect square expression.

Now I'm verifying the correspondence table to make sure the parameters $a = -9/2$ and $b = 9/2$ produce the correct form, and they do. The answer $f(5) = 35$ is a clean integer, which makes sense for this type of problem.

Let me also check whether $\alpha = 3$ was the right choice for the zero of $g$, or if other values like $\alpha = 2$ might work instead. With $\alpha = 2$, I'd get a different relationship between $a$ and $b$, but I need to see if that leads to a valid solution.

Working through the constraint with $\alpha = 2$, I find that $a = -3$ and $b = 2$ is forced, giving me $f(x) = x(x-1)(x-2)$ with $f(5) = 60$. This is actually simpler roots than the $\alpha = 3$ case, and I can verify that $g(x) = \frac{x^2}{4}(x-2)^2$ checks out.

Between the two options, I'll go with $\alpha = 3$ since it's less obvious—even though it introduces a fractional root at $3/2$, the added complexity makes it a better choice. Computing $f(5) = 35$ with this setup.

Now I'm finalizing the problem structure. The PM's design is solid: the problem statement is clean and follows the expected format, the answer of 35 is a nice positive integer, and the uniqueness argument via $(2a+9)^2 \le 0$ is airtight. Both conditions are essential—condition (가) constrains the region of valid $(a,b)$ pairs while condition (나) pins down a specific line, and together they determine the coefficients uniquely. The setup with $f(0)=0$ and leading coefficient 1 is necessary to establish this structure. Let me write out the complete solution to verify all the pieces fit together.

I'm setting up the integral of $f$ to get $g(x)$, which factors nicely as $\frac{x^2}{12}$ times a quadratic in $x$. Since the leading factor is always positive for $x > 0$, condition (가) reduces to requiring that quadratic to be non-negative across all positive $x$. Condition (나) gives me a constraint from evaluating $g$ at $x=3$, which simplifies to a linear relationship between $a$ and $b$.

Now I'm substituting that relationship back into the non-negativity condition and working through the discriminant inequality. This leads me to a quadratic in $a$ that must be non-positive, which factors as a perfect square. Since a perfect square can only be non-positive when it equals zero, I get a unique value for $a$.

From there, I can solve for $b$ and reconstruct the cubic function. Factoring it gives me the roots, so evaluating at $x = 5$ yields the answer. Let me also confirm that assuming $a \ge 0$ creates a contradiction with the constraint that $b \ge 0$, so the negative value of $a$ is indeed the only possibility.

---

[OUTPUT]
# STEP 0. 피드백 반영 선언

이전 Draft 1–4의 **공통 실패 패턴**은 Phase 1 구조 분석 도중 서술이 잘려 Phase 3까지 도달하지 못한 것입니다. 이번에는 **각 Phase를 간결하되 완결적으로** 기술하여 반드시 STEP 3까지 출력을 완료합니다. 특히 분기 분석과 유일성 증명은 핵심만 남기고 불필요한 반복 전개를 삼갑니다.

---

# STEP 1. 구조 분석

## 1-1. 조건이 만드는 방정식 체계

$f(x) = x^3 + ax^2 + bx$ (최고차항 계수 $1$, $f(0) = 0$)

$$g(x) = \int_0^x f(t)\,dt = \frac{x^4}{4} + \frac{ax^3}{3} + \frac{bx^2}{2} = \frac{x^2}{12}\bigl(3x^2 + 4ax + 6b\bigr)$$

$Q(x) = 3x^2 + 4ax + 6b$로 놓으면

$$g(x) = \frac{x^2}{12}\,Q(x)$$

**(가)** $g(x) \ge 0\;(\forall\, x > 0)$

$x > 0$일 때 $\dfrac{x^2}{12} > 0$이므로 **(가) ⟺ $Q(x) \ge 0\;(\forall\, x > 0)$.**

$Q$는 이차계수 $3 > 0$인 위볼록 포물선이므로, $Q$가 양의 실수 영역에서 음수가 되려면 **양의 근**이 존재해야 함. 따라서

$$\text{(가)} \;\Longleftrightarrow\; Q\text{가 양의 근을 갖지 않음.} \tag{★}$$

$Q$의 꼭짓점: $x_v = -\dfrac{2a}{3}$, 판별식 $\Delta_Q = 16a^2 - 72b$.

- **경우 I** ($a \ge 0$): $x_v \le 0$이므로 $Q$는 $x > 0$에서 단조증가. $Q(0) = 6b \ge 0$이면 충분. 즉 **$b \ge 0$**.
- **경우 II** ($a < 0$): $x_v > 0$이므로 최솟값 $Q(x_v) = 6b - \dfrac{4a^2}{3} \ge 0$, 즉 **$b \ge \dfrac{2a^2}{9}$**.

**(나)** $g(3) = 0$

$$\frac{9}{12}\bigl(27 + 12a + 6b\bigr) = 0 \;\Longrightarrow\; 9 + 4a + 2b = 0 \;\Longrightarrow\; b = -\frac{9 + 4a}{2} \tag{L}$$

## 1-2. 분기 분석

**(나)의 직선 (L)을 각 경우에 대입)**

| 경우 | (가) 조건 | (L) 대입 결과 |
|---|---|---|
| I: $a \ge 0$ | $b \ge 0$ | $b = -\frac{9+4a}{2} < 0$ → **모순** (탈락) |
| II: $a < 0$ | $b \ge \frac{2a^2}{9}$ | $-\frac{9+4a}{2} \ge \frac{2a^2}{9}$ → 아래 전개 |

경우 II 전개:

$$-\frac{9+4a}{2} \ge \frac{2a^2}{9} \;\Longrightarrow\; -9(9+4a) \ge 4a^2 \;\Longrightarrow\; 4a^2 + 36a + 81 \le 0$$

$$\Longrightarrow\; (2a + 9)^2 \le 0$$

$(2a+9)^2 \ge 0$이므로 **등호만 성립**: $\;a = -\dfrac{9}{2}$.

### 판정 1-2

분기 I 탈락, 분기 II에서 유일해 $a = -\tfrac{9}{2}$ 확정. **CONTINUE.**

## 1-3. 자유도 계산

| 파라미터 | 초기 자유도 | 소진 과정 |
|---|---|---|
| $a$ | 1 | (가)+(나) 연립 → $(2a+9)^2 = 0$으로 유일 결정 |
| $b$ | 1 | (L)에 의해 $b = \tfrac{9}{2}$로 종속 |

**자유도 = 0**. $f$가 유일하게 결정됨.

### 판정 1-3

자유도 0, 유일해 존재 → **CONTINUE.**

## 1-4. 탐색 전략 수립

유일해가 이미 기호적으로 결정되었으므로 Phase 2에서 수치 대입 후 $f(5)$를 계산하고, 정수성·손계산 적합성을 확인한다. **CONTINUE.**

---

# STEP 2. 수치 탐색 + 포장 설계

## 2-1. 유일해의 수치 확인

$$a = -\frac{9}{2}, \quad b = -\frac{9 + 4(-9/2)}{2} = -\frac{9-18}{2} = \frac{9}{2}$$

$$f(x) = x^3 - \frac{9}{2}x^2 + \frac{9}{2}x = x\!\left(x^2 - \frac{9}{2}x + \frac{9}{2}\right)$$

근 분해: $x^2 - \dfrac{9}{2}x + \dfrac{9}{2} = 0$의 근 $= \dfrac{9/2 \pm 3/2}{2} = 3,\;\dfrac{3}{2}$

$$\boxed{f(x) = x\!\left(x - \frac{3}{2}\right)\!(x - 3)}$$

**검증 — $g(x)$ 구조:**

$$g(x) = \frac{x^2}{12}\bigl(3x^2 - 18x + 27\bigr) = \frac{x^2}{12}\cdot 3(x-3)^2 = \frac{x^2(x-3)^2}{4}$$

- (가) $g(x) = \dfrac{x^2(x-3)^2}{4} \ge 0\;(\forall\, x > 0)$ ✔
- (나) $g(3) = \dfrac{9 \cdot 0}{4} = 0$ ✔

## 2-2. 발문 후보

| 발문 | 값 | 정수성 | 비고 |
|---|---|---|---|
| $f(5)$ | $5 \cdot \frac{7}{2} \cdot 2 = 35$ | ✔ | PM 원안, 깔끔 |
| $f(4)$ | $4 \cdot \frac{5}{2} \cdot 1 = 10$ | ✔ | 대안 |
| $f(-1)$ | $(-1)\cdot(-\frac{5}{2})\cdot(-4) = -10$ | ✔ | 음수 답 |

**채택:** PM 원안 $f(5) = 35$. 양의 정수, 두 자릿수, 손계산 3초.

## 2-3. 포장 설계

### 포장 결정 사항

| 항목 | 결정 | 근거 |
|---|---|---|
| 발문 | $f(5)$의 값 | 정수 35, 자연스러움 |
| (가) 표면 형태 | "$g(x) \ge 0$" 부등식 | 부등식 ↔ 등식 연립이 핵심 메커니즘 — 그대로 노출 |
| (나) 표면 형태 | "$g(3) = 0$" 등식 | 특정 점 지정, (가)와 대조적 형태 |
| 변수 은닉 | $Q(x)$ 분리, $(2a+9)^2 \le 0$ 발견을 학생에게 위임 | 핵심 사고 단계 |

### 유일성 확인

$(2a+9)^2 \le 0$에서 $a = -9/2$가 **유일**. $b$는 종속. 복수해 없음. ✔

### 손계산 확인

- $g(x)$ 전개: 4줄 이내
- (L) 도출: 1줄
- (가)에 대입 → 완전제곱 발견: 3줄
- $f(5)$ 계산: 1줄
- **총 10줄 내외**, 수능 30번 적합 ✔

---

# STEP 3. 최종 검증 + 완성된 문제

## 검증 체크리스트

| 항목 | 판정 |
|---|---|
| 모든 조건이 해 결정에 필요한가 | ✅ (가) 없으면 무한해, (나) 없으면 무한해 |
| 불필요한 조건이 있는가 | ✅ 없음 |
| 수능 어투 준수 | ✅ |
| 발문이 자연스러운가 | ✅ "$f(5)$의 값을 구하시오" |
| PM의 수학적 상황 보존 | ✅ 함수 유형·메커니즘·조건 체계 그대로 |
| 고교 범위 이내 | ✅ 삼차함수, 정적분, 판별식 |
| 답이 자연수 | ✅ 35 |

## 포장 변경 이력

| PM 원안 | 최종안 | 변경 사유 |
|---|---|---|
| (변경 없음) | (변경 없음) | PM 초안의 수치·발문이 모두 정수해·유일성·손계산 조건을 충족하여 그대로 채택 |

---

## ✅ 완성된 문제

> 최고차항의 계수가 $1$이고 $f(0) = 0$인 삼차함수 $f(x)$에 대하여
>
> $$g(x) = \int_0^x f(t)\,dt$$
>
> 라 하자. 함수 $g(x)$가 다음 조건을 만족시킨다.
>
> **(가)** 모든 양수 $x$에 대하여 $g(x) \geq 0$이다.
>
> **(나)** $g(3) = 0$
>
> $f(5)$의 값을 구하시오. **[4점]**

**정답: $35$**