# 생성된 문제
> 2026-04-04T16-11-04

---

## 1. 아이디어 셀렉터 (Idea Maker)

### 수학적 상황
**수학적 객체:** f(x) = x³ + ax² + bx + c (자유 파라미터 a, b, c)

**제약 A (접선–재교차 구조):**
직선 y = x가 곡선 y = f(x)의 점 P(t,t)에서 접선이 되고, P 외에 정확히 한 점 Q에서 곡선과 다시 만난다.

f(t)=t, f'(t)=1이므로 f(x)−x = (x−t)²(x−s). 계수 비교로 b = 1−3t²−2at, c = 2t³+at². **미결: a, t (자유도 2).** b, c는 종속.

**제약 B (합성방정식 근의 개수):**
f(f(x)) = f(0)의 서로 다른 실근이 정확히 N개.

f(y)=c를 풀면 y(y²+ay+b)=0. 수준값 y=0과 이차식의 근 r₁,r₂가 생기고, 각 f(x)=yᵢ의 실근 수는 yᵢ와 f의 극값의 대소에 따라 1/2/3으로 분기. **단독으로 (a,b,c) 공간에서 열린 영역 → 자유도 ≥ 2.**

**공유 파라미터:** a와 t. 제약 A 적용 후 제약 B의 모든 핵심량이 (a,t)의 비선형 함수:
- 이차식 판별식: D = (a+4t)²−4(t²+1)
- 극값 존재 조건: (a+3t)²>3
- 극댓값: a와 t의 삼차식
- 극솟값: a와 t의 삼차식
- 수준값 r₁, r₂: a와 t의 무리식

**강제 결과:** 총 근의 개수 = N을 만족시키려면, 특정 수준값이 극값과 정확히 일치하는 경계 조건이 필요. 이 경계 조건은 a, t에 대한 대수방정식을 생성. 판별식 D의 부호에 따른 케이스 분류에서 대부분의 케이스가 N과 모순되어 탈락하고, 생존하는 유일한 케이스에서 "수준값 = 극값"이라는 두 개의 대수방정식이 (a,t)를 이산적으로 결정. 특히 수준값 r₁이 극솟값과 일치하는 조건과 r₂가 극값 밖에 있는 조건이 동시에 작동하여, a와 t 사이에 예상치 못한 정수비 또는 대칭 관계가 출현.

**영감 source_pid:** P002 (합성방정식 근 분해와 임계값 정렬), P029 (영점 차수 전이 — 이중근 구조가 합성의 수준값 구조를 결정)

---

### 수험생의 발견 순간
1. **처음 시도:** 제약 A를 전개. f(t)=t, f'(t)=1에서 f(x)−x=(x−t)²(x−s)로 인수분해하여 b, c를 a, t로 표현. 여기까지는 순조롭지만 a, t 두 개가 남아 함수가 확정되지 않음.

2. **막히는 순간:** 제약 B로 넘어가 f(f(x))=c를 전개. y(y²+ay+b)=0의 수준값 3개를 구하고, 각각에 대해 f(x)=yᵢ의 근의 수를 세려 하지만, 극댓값·극솟값이 a, t의 복잡한 식이라 대소 비교가 바로 안 됨. 케이스가 여러 개로 갈라져 어디서 시작해야 할지 막막.

3. **연립 과정:** 총 근의 개수 N에서 역추적. 최대 가능 근의 수(3+3+3=9)에서 출발하여, 어떤 수준값이 극값 밖에 있으면 근이 줄어드는 구조를 파악. N을 만족시키려면 정확히 하나의 수준값이 극값 경계에 놓여야 함을 인식. 이 "경계 조건"(수준값 = 극솟값 등)을 a, t로 쓰면 방정식이 하나 추가되고, 판별식 D의 부호 케이스 중 다수가 N과 모순되어 탈락.

4. **발견:** 생존 케이스에서 "수준값 = 극솟값" 방정식과 "D > 0" 조건을 연립하면, a와 t 사이에 깔끔한 관계(예: t = −a/3 + 상수)가 출현하여 (a,t)가 이산적으로 확정. "접선 y=x의 접점 위치 t가, 합성방정식의 수준값이 극솟값과 일치하는 조건에 의해 유일하게 결정되다니!" — 접선의 기하와 합성방정식의 대수가 하나의 등식으로 수렴하는 순간.

---

## 2. 프라블럼 메이커 (Problem Maker)

### 문제 초안
$$\boxed{\text{문제}}$$

최고차항의 계수가 $1$인 삼차함수 $f(x)$에 대하여 다음 조건을 만족시킨다.

> **(가)** 함수 $f(x)$는 극댓값과 극솟값을 모두 갖는다.
>
> **(나)** 직선 $y = x$는 곡선 $y = f(x)$와 점 $(k,\, k)$에서 만나고, $x \ne k$인 점에서 곡선 $y = f(x)$에 접한다.
>
> **(다)** 방정식 $f(f(x)) = f(0)$의 서로 다른 실근의 개수는 $N$이다.

$f(0)$의 값을 구하시오.

\[단답형\]

*(여기서 $k$는 수치 결정 단계에서 확정할 정수, $N$은 수치 결정 단계에서 확정할 자연수)*

---

##

### 조건-상황 대응표
| 조건 | 담당하는 수학적 상황 요소 | 자유도 변화 |
|---|---|---|
| 최고차항의 계수 $1$ | $f(x)=x^3+ax^2+bx+c$ 설정 | 3 |
| **(가)** 극댓값·극솟값 존재 | $f'(x)=0$이 서로 다른 두 실근 → $(t-k)^2>3$ 등 부등식 영역 제한 | 3 (열린 조건, 차원 불변) |
| **(나)** $y=x$가 $(k,k)$에서 교차, 다른 점에서 접선 | $f(x)-x=(x-t)^2(x-k)$: $a,b,c$가 $t$의 함수로 확정 | **1** ($t$ 하나) |
| **(다)** $f(f(x))=f(0)$의 실근 $N$개 | 수준값이 극값 경계에 놓이는 조건 → $t$ 이산 결정 | **0** (유일 해) |
| 발문: $f(0)$ | $c = -t^2 k$ 등 $t$로 표현된 값의 수치 산출 | — |

---

## 3. 디테일 빌더 (Detail Builder)

### 완성된 문제
> 최고차항의 계수가 $1$인 삼차함수 $f(x)$에 대하여 다음 조건을 만족시킨다.
>
> **(가)** 함수 $f(x)$의 극솟값은 $0$보다 작다.
>
> **(나)** 직선 $y = x$는 곡선 $y = f(x)$와 점 $(-1,\,-1)$에서 만나고,
> $x \ne -1$인 점에서 곡선 $y = f(x)$에 접한다.
>
> **(다)** 방정식 $f(f(x)) = f(x)$의 서로 다른 실근의 개수는 $3$이다.
>
> $f(0)$의 값을 구하시오. $\qquad$ [단답형]

$$\boxed{9}$$

---

### 풀이 골격 (채점 참고용)

1. $f(x)=x^3+ax^2+bx+c$로 놓으면 **(나)**에 의해
$$f(x)-x=(x-t)^2(x+1)\quad(t\ne -1)$$
$$\Rightarrow\; a=1-2t,\quad b=(t-1)^2,\quad c=t^2$$

2. **(다)** $f(f(x))=f(x)$ $\Leftrightarrow$ $f(u)=u\;(u=f(x))$
   - $f(u)-u=(u-t)^2(u+1)=0$ → $u=t$ 또는 $u=-1$.
   - $f(x)=-1$: $(x+1)\!\bigl[(x-t)^2+1\bigr]=0$ → $x=-1$ (항상 1개).
   - $f(x)=t$: $(x-t)\!\bigl[x^2-(t-1)x+(1-t)\bigr]=0$, 이차 판별식 $D_2=(t+1)^2-4$.
   - $N=3$ $\;\Rightarrow\; D_2=0$ $\;\Rightarrow\; (t+1)^2=4$ $\;\Rightarrow\; t=1$ 또는 $t=-3$.

3. **(가)** 극솟값 $< 0$ 판별:
   - $t=1$: $f(x)=x^3-x^2+1$, 극솟값 $=\dfrac{23}{27}>0$. ✗
   - $t=-3$: $f(x)=x^3+7x^2+16x+9$, 극솟값 $=f(-2)=-3<0$. ✓

4. $f(0)=c=(-3)^2=\mathbf{9}$.

---

## 4. 이벨류에이터 (Evaluator)

### 원본 문제 A
## Problem

**2.** 두 상수 $a(a \neq 0)$, $b$에 대하여 함수 $f(x) = ax^3 - 6ax^2 + bx$가 다음 조건을 만족시키도록 하는 $a+b$의 값이 $p$ 또는 $q$일 때, $16(p+q)$의 값을 구하시오.

> (가) 방정식 $f(x) = 0$의 서로 다른 실근의 개수는 2이다.
> (나) 방정식 $f(f(x)) = 0$의 서로 다른 실근의 개수는 4이다.

### 원본 문제 B
## Problem

**29.**

상수 $a$$(a > 0)$에 대하여 최고차항의 계수가 자연수인 삼차함수 $f(x)$와 함수

$$g(x) = \int_0^x \left( \frac{3}{2}t^2 + at + a \right) dt$$

가 다음 조건을 만족시킨다.

> (가) 함수 $f(x)$의 극댓값과 극솟값의 차는 4이다.
> (나) 모든 실수 $x$에 대하여 $|f(x) - g(x)| \leq x^2|x - 3|$이다.

$a$의 값이 $p + q\sqrt{3}$일 때, $p + q$의 값을 구하시오.
(단, $p$와 $q$는 유리수이다.)

### 채점
| 항목 | 점수 | 근거 |
|------|:----:|------|
| **융합성** | **5**/5 | 1-2 얽힘 등급: 강한 얽힘 (4/4 O). (나)의 인수분해 $(x-t)^2(x+1)$이 (다)의 $f(u)-u$ 분해에 재사용되어 $D_2=(t-1)(t+3)=0$이라는 **단독 전개에 없던 등식**이 핵심 전환 지점으로 작동. (가)는 이산 해 중 선별 역할. |
| **참신함** | **5**/5 | 원본 A($f(x)=0$, $f(f(x))=0$ 근의 수)와 비교: 객체(접선 조건 부여된 삼차), 조건 구조($f(f(x))=f(x)$로 $f(u)-u$가 접선 구조와 동일), 발문이 모두 다름. 원본 A의 직접적 근 분석 전략으로는 접선 인수분해 재활용 구조를 포착 불가. 원본 B와는 완전히 이질적. |
| **정합성** | **5**/5 | 삼차함수, 접선, 판별식, 합성함수 — 모두 고교 범위. 서술이 자연스럽고 수능 단답형 형식 준수. |
| **논리성** | **5**/5 | 유일해 (1-4). 모든 조건 필수 (1-5): (가) 제거 시 $f(0) \in \{1,9\}$, (나) 제거 시 미결, (다) 제거 시 무한 해. |
| **풀이 깊이** | **5**/5 | 전환 지점 **2회**: ① 접선 인수분해의 합성방정식 재활용 발견, ② $D_2=0$ 경계조건에서 $t$ 이산 결정. 설계 의도의 발견 지점이 풀이에 정확히 실현됨. |
| **총점** | **25/25** | |

---

##